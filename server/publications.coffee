Meteor.publish 'games', ->
  Games.find {}
# Meteor.publish 'viewGames', ->
#   ViewGames.find {}
# Meteor.publish 'structures', ->
#   Structures.find {}
# Meteor.publish 'formulas', ->
#   Formulas.find {}
# postId = "Bc85BruBSduy8KaAj"
# Meteor.publish 'singlePost', postId, ->
  # Formulas.find {_id: postId}
